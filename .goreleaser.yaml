# GoReleaser configuration for Tyk CLI
project_name: tyk-cli

before:
  hooks:
    - go mod tidy
    - go test ./... -short

builds:
  - id: tyk
    binary: tyk
    main: ./cmd/main.go
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildTime={{.Date}}
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: default
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^style:'
      - Merge pull request
      - Merge branch
  groups:
    - title: 'New Features'
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: 'Bug Fixes'
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: 'Performance Improvements'
      regexp: '^.*?perf(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: 'Others'
      order: 999

brews:
  - name: tyk
    repository:
      owner: sedkis
      name: homebrew-tyk
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    homepage: "https://github.com/sedkis/tyk-cli"
    description: "Command-line interface for managing Tyk APIs and configurations"
    license: "MIT"
    dependencies:
      - name: curl
    test: |
      system "#{bin}/tyk --version"
    install: |
      bin.install "tyk"

# GitHub release configuration
release:
  github:
    owner: sedkis
    name: tyk-cli
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## Tyk CLI {{.Tag}}
    
    Welcome to this new release of Tyk CLI! 🎉
    
    ### Quick Install
    ```bash
    # Direct download
    curl -L https://github.com/sedkis/tyk-cli/releases/download/{{.Tag}}/tyk-cli_{{.Tag}}_$(uname -s)_$(uname -m).tar.gz | tar xz
    sudo mv tyk /usr/local/bin/
    
    # Verify installation
    tyk --version
    ```
  footer: |
    **Full Changelog**: https://github.com/sedkis/tyk-cli/compare/{{.PreviousTag}}...{{.Tag}}
    
    ---
    
    🤖 This release was created automatically by [GoReleaser](https://goreleaser.com/).
